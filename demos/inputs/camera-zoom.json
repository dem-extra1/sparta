{
  "_comment": "Camera-keyframe demo (#412): the recorder now interpolates between camera keyframes by tick instead of holding the first. This clip zooms in on the left flank, then pans right and zooms back out, with no input steps -- just the animated framing. Watch the bottom-right distance legend rescale as the zoom changes (the motion #364 wanted but couldn't record before this).",
  "seed": "12345",
  "camera": [
    {"tick": 0,   "x": 700.0, "y": 400.0, "zoom": 1.0},
    {"tick": 90,  "x": 520.0, "y": 300.0, "zoom": 2.2},
    {"tick": 200, "x": 900.0, "y": 460.0, "zoom": 0.9}
  ],
  "steps": []
}
