{
  "_comment": "Dogfoods the new per-PR manifest naming: this file proves the CI resolver picks an ADDED demos/demo.<slug>.json over the pre-existing bare demos/demo.json. Workflow + docs only -- no gameplay change.",
  "skip": true,
  "reason": "CI workflow + docs change (per-PR demo manifest naming) -- no gameplay/visual change for a recorded battle to show."
}
