{
  "skip": true,
  "reason": "Order-response delay is a timing behaviour: units pause ~0.5 s before stepping off. A passive showcase replay has no player orders, so the hesitation is never triggered. A dedicated recording with move orders would be needed to show it."
}
