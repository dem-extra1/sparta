{
  "skip": true,
  "reason": "Non-visual refactor: consolidates the three stance-mode lists in HUD.gd into one _STANCE_ENTRIES source of truth. The control-bar dropup looks and behaves exactly as before, so a recorded battle would show no change --- the win is internal (a one-place update instead of three) and is covered by test_hud_stance.gd."
}
