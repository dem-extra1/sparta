{
  "skip": true,
  "reason": "Externalizes campaign maps to JSON data files and adds a campaign picker on the main menu (#125). This is a campaign-map/data refactor with no tactical-battle behaviour, so a recorded battle replay can't show it."
}
