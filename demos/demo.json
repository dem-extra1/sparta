{
  "skip": true,
  "reason": "CI/tooling change only — adds a GDScript test-coverage job (GUT instrumentation -> lcov -> Codecov). Nothing about how the game looks or plays changes, so a recorded battle clip wouldn't show anything relevant."
}
