{
  "skip": true,
  "reason": "Signal-lifecycle bug fix: disconnects physics_frame in Battle._exit_tree() so a restart no longer null-derefs get_tree().paused. The win is the absence of an error log (and the brief post-restart stutter it caused) on reload_current_scene() --- a recorded battle can't film an error that no longer prints."
}
