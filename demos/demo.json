{
  "skip": true,
  "reason": "Physics slice B adds prone/knockdown as a sim mechanic (a felled soldier loses active defence and can't strike until it rises). Prone soldiers aren't rendered differently yet -- the on-screen effect is just 'charged/flanked troops die faster', not legible standalone -- so the clip is skipped and the behaviour is pinned by tests. A prone visual is a tracked follow-up."
}
