{
  "skip": true,
  "reason": "The floating-blade fix only manifests when a soldier is knocked prone during close combat at figure LOD — a probabilistic event (knockdown depends on a random impulse roll) that can't be triggered deterministically from a scripted input."
}
