{
  "input": "demos/inputs/conversio.json",
  "caption": "Conversio (V key) rendered through the unified path: marks now draw directly from the simulated body positions (the cosmetic spring layer is gone), so the per-soldier about-face renders cleanly with no separate render-side state to keep in sync.",
  "fixed_fps": 30,
  "max_frames": 300,
  "fps": 12,
  "width": 640
}
