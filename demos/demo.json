{
  "skip": true,
  "reason": "Input-affordance change: a unit's raised flag is now a click target for selection/attack, just like its block. The flag already renders the same, and a passive recorded battle can't show the player clicking it, so a clip would look identical to before. Covered by the SelectionManager flag-hit unit tests instead."
}
