{
  "skip": true,
  "reason": "Pure loader validation: allows intentional one-way province edges via a 'one_way' flag and warns (instead of rejecting) on un-flagged asymmetric adjacency (#148, revisiting #128). No scene, HUD, or gameplay change — a recorded battle can't demonstrate a parse-time schema check."
}
