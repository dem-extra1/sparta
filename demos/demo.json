{
  "skip": true,
  "reason": "Wires the campaign map to launch the real tactical battle for a contested province clash, sizing each army from the clashing strengths and applying the winner + survivors back to the map (#122). The demo recorder replays a single saved battle file, which has no campaign context, so it can't show the map->battle->map hand-off or the strength-scaled deployment that defines this change."
}
