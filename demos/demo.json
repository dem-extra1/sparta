{
  "skip": true,
  "reason": "Individual-collision phase 2: the engaged-tier classification (with hysteresis) and the deterministic soldier-level separation primitive, still behind the off-by-default INDIVIDUAL_COLLISION flag. Nothing in the simulation reads the layer yet, so no gameplay or visual change — a recorded battle would look identical."
}
