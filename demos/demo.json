{
  "skip": true,
  "reason": "Pure performance refactor of the friendly-collision broadphase: each regiment's soldier_block_extent() is now precomputed once per tick instead of recomputed (and reallocated) per regiment pair. Behaviour is byte-identical -- the SoldierSteering tests pin it -- so a recorded battle would look exactly the same."
}
